# 网站定制指南

本指南将帮助你将这个arXiv论文聚合网站改造成你自己的网站。

## 📋 需要修改的文件清单

### 1. 网站基础配置

#### `docusaurus.config.js` - 核心配置文件

需要修改的内容：

- **第13行**: `title: 'DarkKnight Note'` → 改为你的网站标题
- **第14行**: `tagline: 'AI Dark Knight Note'` → 改为你的网站副标题
- **第23行**: `url: 'https://darkknight996.github.io'` → 改为你的网站URL
- **第30行**: `organizationName: 'DarkKnight996'` → 改为你的GitHub用户名
- **第31行**: `projectName: 'DarkKnight996.github.io'` → 改为你的仓库名
- **第41行**: `defaultLocale: 'en'` → 如果网站是中文，改为 `'zh-Hans'`
- **第42行**: `locales: ['en']` → 如果网站是中文，改为 `['zh-Hans']`
- **第69-74行**: Giscus评论系统配置
  - `repo: 'DarkKnight996/comment'` → 改为你的评论仓库
  - `repoId: 'R_kgDOPH6giw'` → 改为你的仓库ID（需要从Giscus获取）
  - `category: 'General'` → 改为你的分类名
  - `categoryId: 'DIC_kwDOPH6gi84Csjmd'` → 改为你的分类ID（需要从Giscus获取）
- **第83行**: `title: 'Dark Knight Note'` → 改为你的导航栏标题
- **第85行**: `alt: 'DarkKnight Note'` → 改为你的logo描述
- **第90行**: `href: 'https://github.com/DarkKnight996'` → 改为你的GitHub链接
- **第98行**: `Copyright © ${new Date().getFullYear()} DarkKnight996, Inc.` → 改为你的版权信息

#### `package.json` - 项目信息

- **第2行**: `"name": "my-website"` → 改为你的项目名称

### 2. 评论系统配置（Giscus）

#### 设置步骤：

1. 访问 [Giscus](https://giscus.app/)
2. 使用GitHub账号登录
3. 选择或创建一个用于评论的仓库（建议创建一个专门的评论仓库）
4. 配置完成后，Giscus会提供：
   - `repo`: 仓库名（格式：`username/repo-name`）
   - `repoId`: 仓库ID
   - `category`: 分类名
   - `categoryId`: 分类ID
5. 将这些信息填入 `docusaurus.config.js` 的 `giscus` 配置中

### 3. Python脚本配置

#### `get_daily_arxiv_paper.py` - 论文获取脚本

需要修改的内容：

- **第125-127行**: OpenAI客户端配置
  ```python
  self.client = OpenAI(
      api_key=os.environ.get('DEEPSEEK_API_KEY'),
      base_url="https://api.deepseek.com"
  )
  ```
  如果你使用其他LLM服务（如OpenAI、Claude等），需要修改：
  - `DEEPSEEK_API_KEY` → 改为你的环境变量名
  - `base_url` → 改为对应的API地址

- **第137行**: 论文分类筛选
  ```python
  def fetch_arxiv_papers(self, categories=['cs.DC', 'cs.AI'], ...):
  ```
  修改为你感兴趣的arXiv分类，例如：
  - `cs.AI` - 人工智能
  - `cs.LG` - 机器学习
  - `cs.CV` - 计算机视觉
  - `cs.CL` - 计算语言学
  - `cs.NE` - 神经网络与进化计算
  - 等等（完整列表见：https://arxiv.org/category_taxonomy）

- **第187-199行**: 论文筛选逻辑
  当前逻辑：
  - `cs.DC` 分类的论文全部收录
  - `cs.AI`/`cs.LG` 分类的论文需要摘要中包含 "reinforcement learning" 或 "accelerate"
  
  你可以根据需求修改筛选条件。

- **第443-466行**: LLM提示词（Prompt）
  这个提示词用于让LLM分析论文并生成标签、机构、摘要等信息。
  你可以根据需求修改：
  - 标签分类规则
  - 输出格式
  - 摘要长度等

- **第902行**: 主函数中的分类配置
  ```python
  categories=['cs.DC', 'cs.AI', 'cs.LG']
  ```
  修改为你需要的分类。

### 4. 网站内容

#### `docs/intro.md` - 首页介绍

修改为你的个人介绍和网站说明。

#### `src/pages/index.js` - 首页重定向

当前重定向到 `/intro`，如果需要可以修改。

### 5. 网站样式

#### `src/css/custom.css` - 自定义样式

可以在这里添加你的自定义CSS样式。

#### `static/img/` - 图片资源

替换以下文件：
- `favicon.ico` - 网站图标
- `logo.svg` - Logo文件
- `docusaurus-social-card.jpg` - 社交媒体卡片图片

### 6. 环境变量配置

创建 `.env` 文件（不要提交到Git）或设置系统环境变量：

```bash
DEEPSEEK_API_KEY=your_api_key_here
```

如果使用其他LLM服务，修改对应的环境变量名。

### 7. 部署配置

#### GitHub Pages部署

1. 在GitHub上创建仓库（如果使用GitHub Pages）
2. 修改 `docusaurus.config.js` 中的 `url` 和 `baseUrl`
3. 如果仓库名不是 `username.github.io`，需要设置 `baseUrl: '/repository-name/'`

#### 其他部署方式

- Vercel
- Netlify
- 自己的服务器

## 🚀 快速开始步骤

1. **克隆或Fork项目**
   ```bash
   git clone <your-repo-url>
   cd <your-repo-name>
   ```

2. **修改基础配置**
   - 修改 `docusaurus.config.js` 中的网站信息
   - 修改 `package.json` 中的项目名称

3. **设置评论系统**
   - 访问 Giscus 并配置
   - 更新 `docusaurus.config.js` 中的 Giscus 配置

4. **配置Python脚本**
   - 设置环境变量（API密钥）
   - 修改论文分类和筛选逻辑
   - 根据需要调整LLM提示词

5. **替换资源文件**
   - 替换Logo和图标
   - 修改首页内容

6. **测试运行**
   ```bash
   # 安装依赖
   npm install
   # 或
   yarn install
   
   # 启动开发服务器
   npm start
   # 或
   yarn start
   ```

7. **测试Python脚本**
   ```bash
   # 安装Python依赖
   pip install -r requirements.txt
   
   # 设置环境变量
   export DEEPSEEK_API_KEY=your_key
   
   # 运行脚本（测试模式，限制论文数量）
   python get_daily_arxiv_paper.py
   ```

8. **构建和部署**
   ```bash
   npm run build
   # 或
   yarn build
   ```

## 📝 定时任务设置

### GitHub Actions（推荐）

项目已包含 `.github/workflows/update_arxiv.yml` 配置文件，需要修改：

1. **设置GitHub Secrets**（在仓库 Settings → Secrets and variables → Actions）：
   - `DEEPSEEK_API_KEY`: 你的DeepSeek API密钥（或其他LLM服务的API密钥）
   - `PAT_TOKEN`: 个人访问令牌（Personal Access Token），用于推送代码
     - 创建方法：GitHub Settings → Developer settings → Personal access tokens → Tokens (classic)
     - 需要 `repo` 权限
   - `ACCESS_TOKEN`: 用于部署GitHub Pages的访问令牌（如果使用自动部署）

2. **修改定时触发时间**（`.github/workflows/update_arxiv.yml` 第5行）：
   ```yaml
   - cron: '0 1,2,3,4,7,10 * * *'  # UTC时间，对应北京时间 09,10,11,12,15,18 点
   ```
   Cron格式：`分钟 小时 日 月 星期`
   - 北京时间 = UTC时间 + 8小时
   - 例如：每天北京时间早上9点 = `cron: '0 1 * * *'`（UTC 01:00）

3. **修改工作流名称和描述**（可选）

### 其他定时任务方式

- **Cron Job**（Linux/Mac）
  - 在服务器上设置cron任务

- **Windows任务计划程序**
  - 在Windows上设置定时任务

## ⚠️ 注意事项

1. **API密钥安全**
   - 不要将API密钥提交到Git仓库
   - 使用环境变量或密钥管理服务

2. **API调用成本**
   - LLM API调用可能产生费用
   - 建议先测试少量论文，确认成本可接受

3. **论文筛选逻辑**
   - 根据你的需求调整筛选条件
   - 避免收录过多不相关的论文

4. **文件大小**
   - 定期清理 `temp_pdfs` 目录（如果使用）
   - 注意Markdown文件大小，避免过大

## 🔧 高级定制

### 修改论文展示格式

编辑 `get_daily_arxiv_paper.py` 中的 `format_paper_with_enhanced_info` 方法（第598行）。

### 添加新的文档分类

1. 在 `docs/` 下创建新目录
2. 在 `sidebars.js` 中配置（如果需要手动配置）

### 自定义主题

参考 Docusaurus 官方文档：https://docusaurus.io/docs/styling-layout

## 📚 相关资源

- [Docusaurus 文档](https://docusaurus.io/docs)
- [arXiv 分类列表](https://arxiv.org/category_taxonomy)
- [Giscus 文档](https://github.com/giscus/giscus)
- [DeepSeek API 文档](https://platform.deepseek.com/api-docs/)

## ❓ 常见问题

**Q: 如何修改论文分类？**
A: 修改 `get_daily_arxiv_paper.py` 中 `fetch_arxiv_papers` 方法的 `categories` 参数。

**Q: 如何更换LLM服务？**
A: 修改 `get_daily_arxiv_paper.py` 中 `__init__` 方法的客户端初始化代码，以及 `call_api_for_tags_institution_interest` 方法中的API调用。

**Q: 评论系统不显示？**
A: 检查 `docusaurus.config.js` 中的 Giscus 配置是否正确，确保仓库已启用 Discussions 功能。

**Q: 如何设置定时任务？**
A: 可以使用 GitHub Actions，创建 workflow 文件来定时运行Python脚本。

---

祝你定制顺利！如有问题，欢迎查看相关文档或提交Issue。

